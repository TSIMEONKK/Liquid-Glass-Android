<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 读取图片权限 (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- 读取外部存储权限 (Android 12 及以下) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiquidGlass"
        tools:targetApi="31">

        <!-- LiquidGlass 专业演示 Activity (主入口)
             注意：namespace 为 com.example.liquidglass.demo，
             Activity 类仍在 com.example.liquidglass 包下，需写全名 -->
        <activity
            android:name="com.example.liquidglass.ProfessionalDemoActivity"
            android:exported="true"
            android:label="LiquidGlass Professional Demo"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- README 首屏素材录制用：iOS 壁纸 + 图标网格 + 液态玻璃药丸 -->
        <activity
            android:name="com.example.liquidglass.HeroShowcaseActivity"
            android:exported="true"
            android:label="LiquidGlass Hero"
            android:screenOrientation="portrait" />

        <!-- 原生 WebView 与玻璃取样的独立对照页，仅从 Demo App 内进入。 -->
        <activity
            android:name="com.example.liquidglass.WebViewReproActivity"
            android:exported="false"
            android:label="@string/webview_repro_title"
            android:screenOrientation="portrait" />

    </application>

</manifest>
